package io.aranea.events;

/**
 * Classes implementing this interface represent specific events and their
 * meaning / significance, data structure (fields),
 * data types, properties and constraints
 */
public interface Event {

    // read from serialized form and validate

    // validate and write to serialized form
}
